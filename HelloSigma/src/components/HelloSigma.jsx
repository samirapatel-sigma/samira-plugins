import React from 'react';
import PropTypes from 'prop-types';

// Basic component to display 'hello sigma'. Additional props can be added as needed.
const HelloSigma = () => {
  return (
    <h1 className="text-3xl font-bold text-gray-800">hello sigma</h1>
  );
};

HelloSigma.propTypes = {};

export default HelloSigma;