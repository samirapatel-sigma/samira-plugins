import React from 'react';

/**
 * App Component - Renders a simple plugin element that displays text.
 */
const App = () => {
  return (
    <main style={{ minHeight: '100vh', display: 'flex', justifyContent: 'center', alignItems: 'center', fontSize: 24 }}>
      Hello Sigma World
    </main>
  );
};

export default App;